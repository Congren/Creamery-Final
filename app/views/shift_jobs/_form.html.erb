<%= simple_form_for(@shift_job) do |f| %>
  <% if @shift_job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shift_job.errors.count, "error") %> prohibited this shift_job from being saved:</h2>

      <ul>
      <% @shift_job.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :job_id, :collection => Job.active.alphabetical, :include_blank => false, input_html: {class: "avg"} %>
  </div>
  <div class="field">
    <%= f.input :shift_id, :collection => Shift.upcoming.order(:id), :include_blank => false, input_html: {class: "avg"} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
